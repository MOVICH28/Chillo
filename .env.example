# Database
DATABASE_URL="postgresql://user:password@host/dbname?sslmode=require"

# Solana
NEXT_PUBLIC_SOLANA_NETWORK="devnet"
NEXT_PUBLIC_SOLANA_RPC_URL="https://api.devnet.solana.com"
PLATFORM_PRIVATE_KEY=""        # base58 or JSON array — platform wallet keypair

# Optional API keys
HELIUS_API_KEY=""

# Cron auth — used for manual testing (curl -H "Authorization: Bearer ...")
CRON_SECRET=""

# QStash — from console.upstash.com → QStash → Signing Keys
QSTASH_CURRENT_SIGNING_KEY="sig_6iUW8tdTScBEeVsRRuwPeUSdbd4z"
QSTASH_NEXT_SIGNING_KEY="sig_4zQtndK61nDB3xATRq8QENt561sB"
